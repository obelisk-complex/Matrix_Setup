# Base compose fragment — network and shared volumes
# Generated by Matrix Stack Setup

# @section networks
  matrix-net:
    driver: bridge

# @section volumes
  postgres-data:
  caddy-data:
  caddy-config:
