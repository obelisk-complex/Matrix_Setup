# Synapse Admin UI compose fragment
# Generated by Matrix Stack Setup

services:
  synapse-admin:
    image: {{SYNAPSE_ADMIN_IMAGE}}
    container_name: matrix-synapse-admin
    restart: unless-stopped
    networks:
      - matrix-net
    depends_on:
      homeserver:
        condition: service_healthy
    environment:
      REACT_APP_SERVER: "https://{{DOMAIN}}"
